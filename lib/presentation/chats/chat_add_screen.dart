import 'package:flutter/material.dart';

class ChatAddScreen extends StatelessWidget {
  const ChatAddScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Add Chat'),
    );
  }
}
